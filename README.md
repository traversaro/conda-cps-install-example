# conda-cps-install-example

Example of how CPS files are installed by default in a conda environment.

To run, install `pixi` and then run:

~~~
git clone https://github.com/traversaro/conda-cps-install-example
cd conda-cps-install-example
pixi run list-installed-cps
~~~
